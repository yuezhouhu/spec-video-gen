# SDVG：ICLR 2027 投稿前关键修改建议

审阅对象：`2026_SDVG_ICLR.pdf`，共 12 页。审阅日期：2026-09-25。下文页码均为 PDF 页码。已阅读全文、表格和定性图；未获得实验代码、逐样本结果或完整视频，因此实现风险与统计显著性问题均是待核验项，不当作已经证实的代码错误。另核对了 RSD、HybridStitch 的公开摘要，以及 VisionReward 的公开论文；这不构成截至投稿日的完整新颖性检索。

**总体判断：选题有价值，但当前版本最影响接收的，是核心贡献的实验归因不足、比较对象不够强，以及“接近 target 质量”的证据偏弱。优先补决定论文主张是否成立的证据，再润色文字。**

有利基础是：方法简单、无需训练、使用了 1003 个 prompt、报告端到端时间，并且用不同于路由器的 VisionReward 评价。最值得深挖的发现，是首块的大模型生成对后续质量有很强影响。这可能成为更有说服力的论文主线。

## 1. P0：先证明 reward routing 比“首块大模型＋简单分配”更有价值

**证据：第 8 页表 2。**

- 完整 SDVG：VR 0.0773，60.9 s，1.59×。
- Force-reject + random routing：VR 0.0771，58.2 s，1.67×。
- Force-reject only, no verification：VR 0.0757，43.8 s，2.21×。
- Draft-only：VR 0.0644，25.7 s。

完整方法相对“首块大模型＋随机路由”仅高 0.0002，却更慢 2.7 s。这个差异是否可靠，目前没有置信区间支持。仅首块使用 target，就覆盖了完整 SDVG 相对 draft-only 的约 87.6% 的 VR 数值增益：`(0.0757−0.0644)/(0.0773−0.0644)`。这是描述性差值分解，不能解释成已经严格识别了因果贡献比例。

第 7 页先使用未保护首块的 random routing（0.0706）论证 reward 的重要性，混入了首块策略的影响；后文虽承认首块贡献很大，仍未建立路由器的独立价值。

**必须补的实验：**

1. 所有方法统一首块 target，比较 reward、random、固定间隔 target、前 m 块 target、仅首块 target；对 random 扫完整预算曲线。
2. 做两种公平匹配：相同 target 计算量用于归因；相同端到端时间用于实际竞争力。随机方法省去的 reward 计算应允许用于生成。
3. hybrid 同样增加“随机选块＋相同 stitching”以及“所有后续块固定 stitching”的对照，避免将 stitching 的收益误记为 reward 的收益。
4. 所有主要比较使用相同 prompt 和按 `(prompt, seed, block, noise purpose)` 固定的随机数流；以 prompt 为单位做配对 bootstrap。资源允许时增加至少 3 个 seed，并区分 prompt 不确定性与生成随机性。

**若路由器优势仍很小：** 不要靠扩大措辞维持主张。可以把主线改为“首块锚定与按块计算分配”，诚实展示简单基线，并把路由作为高质量端的可选增强；但该主线也需要跨模型、跨时长和机制实验支撑。

## 2. P0：把强基线放入主图，重新判断 Pareto 优势

**证据：第 6 页表 1、第 7 页图 3、第 8 页表 2。**

图 3 只有两个 SDVG 系列，没有最关键的简单对照。按已报告的 VR 与时间：

- 仅首块 target 的 0.0757 / 43.8 s，与 SDVG τ=−1.5 的 0.0757 / 51.6 s 同分但更快。
- 它也同时优于 SDVG τ=−2.0 的 0.0756 / 47.4 s，以及 hybrid τ=−2.0 的 0.0751 / 46.1 s。

因此，至少在论文当前使用的这两个坐标上，部分主结果不是所有已报告方法构成的 Pareto 前沿。这个判断只针对表中点估计，不推断其他未报告质量维度。

**建议：** 将上述简单基线和预算曲线放入主图，而不是仅放消融；加入可公平适配的 T-Stitch、SRDiffusion、HybridStitch 对照。至少实现一个在同一 AR 模型对、同一 4-step schedule 下的固定 stitching 强基线。对无法直接适配的工作说明限制，不能用“原本不是为 AR 设计”代替实验。

**成功标准：** 在某个具有实际意义的延迟区间，方法在独立视频质量指标或人评上稳定优于最强简单基线；不要求所有预算都赢。

## 3. P0：不要把 reward 分数比值当成整体感知质量百分比

**证据：摘要；第 6 页表 1。**

“98.1% quality retention”来自 `0.0773/0.0788`。除非论证该分数有适合做比例解释的零点，否则这一比值不能直接表示保留了 98.1% 的人类感知质量。VisionReward 是由视觉判断与偏好学习构成的评分，不能自动视为质量的比例尺。

同一表中，warp error 从 target 的 0.0066 增至 SDVG τ=−0.7 的 0.0085，约增加 28.8%；hybrid τ=−1.0 为 0.0091，约增加 37.9%。这不等价于人眼质量下降相同比例，但说明时序误差确有明显相对变化，摘要不宜用一个接近 100% 的数概括。

**建议：**

- 摘要直接写 VR 绝对值与 speedup；如保留比例，明确写“98.1% of the target's VisionReward score”。
- 补完整视频的盲测，覆盖 SDVG、hybrid、target、首块 target 基线及预算匹配的随机/固定策略。左右随机，允许平局，评价整体质量、动作自然性、时序一致性和文本对齐。
- 可先做 150–200 个分层抽样 prompt、每对至少 3 位独立评价者的初步研究，再根据置信区间决定样本量；这个数量是启动建议，不是显著性保证。
- 报告视频指标的逐维结果、置信区间、差样本比例和低分尾部；补 VBench 或相当的时序、运动、主体一致性指标。若想主张“接近 target”，预先定义有依据的非劣效界值。
- 复核 EvalCrafter 的原始指标定义、版本和聚合权重。表 1 按拟合聚合权重标注 IS↓、action↓、flow↓，但负回归系数不自动改变原始指标的语义。将原始指标和拟合总分分开，尤其避免把较低运动量直接解释为更好视频。此处需要核对代码，本次未成功获取 EvalCrafter 官方说明，不能断言所有箭头都错。

## 4. P0：核验边界配置，补全缓存与随机数语义

**证据：第 4 页 §3.1、图 2；第 8 页表 3；第 9 页 k=4 解释。**

稿件写接受 latent 后直接“committed to the target KV cache”，但没有描述两套模型各自如何处理最终确认的历史。1.3B 与 14B 一般不能直接共享数值 K/V；共享已生成的视频历史，不等于共享同一组 K/V 张量。

需要给出包含以下内容的算法框：

- 独立维护 `K_D`、`K_T`；最终选定的 block 如何经各自模型更新 cache。
- 接受 draft 时 target 是否需要额外 prefill / context forward，拒绝时 drafter 是否也需要重新消费 target 输出，以及这些成本是否已计时。
- 当前 draft 候选产生的临时 cache 如何提交或回滚；滑动窗口、位置索引和 context noise level 如何处理。
- 若 causal VAE 使用流式状态，候选解码被拒绝时如何回滚，避免候选污染随后真正输出的解码状态。
- block 0 直接由 target 生成即可；既然必拒绝，应跳过其 drafter、router 和候选解码，并重新计时。

**一个很有价值的正确性检查：** 按式 (2) 和第 9 页文字，首块受保护且 k=4 时，后续 rejected block 也最终使用完整 draft，应该在固定相同随机数流、cache 语义与解码状态后退化为“首块 target，其余 draft”。但表 3 给出 0.0752，表 2 的无验证版本给出 0.0757。若 block 0 也 k=4，则应退化为 draft-only；表中对应为 0.0646 与 0.0644。不能仅据差异判定 bug，但应逐 latent 检查一致性，解释随机数消费、实现路径或运行非确定性造成的差异，而非只说明都用了 seed=42。

另外，表 2 注释说 acceptance 以 9 块为分母且包含必拒绝的首块，此时最大应为 8/9≈88.9%，不能将“仅首块拒绝”标为 100.0%。第 7 页“Every run keeps force-reject-block-0”也与未保护首块的 random ablation 冲突。

## 5. P1：让路由器预测“target 能修复多少”，而不只是 draft 的低分

**证据：第 4 页式 (1)、第 7–9 页消融讨论。**

低 ImageReward 可能表示 prompt 本来就难、图像模型不理解该视频瞬间、或 target 同样失败。反之，帧看起来都漂亮的 block 也可能有运动冻结、跨块跳变或主体漂移。仅以分数判断需要大模型，尚缺少机制证据。

最值得补的是一个反事实实验：固定同一历史、同一初始噪声，分别生成 draft block 与 target/hybrid block，并对两条分支用同一后续策略继续生成。分别量化即时和下游质量收益，考察当前 reward 与这些收益的关系。

可将预算分配原则表述为：优先给预期质量增益相对额外成本较高的 block 分配 target 计算。即考察 `E[ΔQ | history, candidate] / ΔC`，并将未来 block 的影响计入 `ΔQ`。这是一种应验证的决策建模，不是当前阈值规则已有的最优性定理。

由此决定是否增加廉价的跨块主体一致性、边界差异或运动异常信号。不要仅为显得复杂而堆叠 reward。若继续强调 training-free，区分无需更新参数与完全无需校准。

## 6. P1：级联评分有明确的单边结构，应据此修改

**证据：第 5 页式 (3)。**

同一候选的 4 帧集合是完整 12 帧集合的子集，且每帧使用相同评分方式时：

`q̂ = min(subset scores) ≥ q = min(all scores)`。

因此 `q̂ < τ` 已足够安全拒绝，无需等到 `τ−δ`；而 `q̂ ≥ τ+δ` 并不能保证接受，因为未采样的 8 帧仍可能低分。当前对称 ambiguity band 没有利用这个结构，且早接受与完整 min-frame 决策并不等价。

**建议：** 做单边早拒绝；对早接受在独立验证集上校准，报告“相对全帧规则的假接受率”、缺陷漏检率、决策一致率、时延，以及 δ 的具体值和敏感性。9.1% full-pass rate 只是成本统计，不是可靠性证据。复用已计算的 4 帧分数，并区分省下的是 reward 编码、reward 推理还是 VAE 解码。

还要承认 cascade 与“任何单帧坏掉即拒绝”的核心动机之间存在取舍。全文应统一说明：完整规则是 worst-frame 策略，cascade 是其近似。

## 7. P1：重新明确新颖性，并把首块效应做成机制发现

**证据：第 2–3 页相关工作及贡献；第 9 页首块消融。**

RSD 已使用 reward、阈值和 draft/target 动态选择。因此，当前稿件容易被理解为“将 PRM 换成 ImageReward，把 reasoning step 换成 video block”。单靠连续空间没有 logits 的表述，不能解释为什么视频版本需要新的方法贡献。

与此同时，已核对的 HybridStitch 摘要明确包含按生成难度划分空间区域、让大模型细化复杂区域的机制。第 2 页将其概括为仅 timestep 维度、第 3 页笼统称这些工作 content-agnostic，会削弱相关工作的可信度。应精准说明差异是因果视频历史、逐块反馈及跨块误差传播，而不是否认相关工作已有的自适应性。

建议主张分三层建立：

1. **发现：** target 计算在不同 block 的收益不均匀，首块具有持续的下游影响。
2. **方法：** 在首块得到高质量初始化后，依据可验证的修复收益分配后续 target 计算，并复用 draft 轨迹。
3. **证据：** 在相同预算下优于固定分配，跨模型对、时长和 prompt 分布成立。

机制实验可以固定相同的 target 调用次数，只改变其所在 block；再交换首块来源，固定后续策略，画质量随 block index 的变化。这能区分首块是否真的特殊，而不是笼统地“多用大模型更好”。

标题可考虑 **“Reward-Guided Model Collaboration for Autoregressive Video Generation”**；若保留 SDVG，建议明确使用 reward-guided / approximate speculative generation，并在摘要中说明不保持 target 分布。无需为了像经典 speculative decoding 而强加不可支持的理论保证。

## 8. P1：补足泛化和流式性能，限定 drop-in 的范围

**证据：第 5 页 §4.1；第 9 页结论。**

当前只验证一个模型对、一个分辨率、9 blocks 和 seed=42，不足以支持“any Self-Forcing-style pair”的广泛表述。

- 至少补另一组可兼容模型/检查点；不能做到时，明确结论仅对该模型对成立。
- 同一模型对测试更长视频，例如 9/18/36 blocks，验证首块优势与路由阈值能否跨长度保持。记录身份漂移、运动冻结、错误的累积与恢复。
- 在独立验证 prompt 上确定 τ、δ，测试时冻结；新增动作密集、多人交互、遮挡和镜头运动的分组结果。只扫同一个测试集不能建立 calibration-free 主张。
- 补 TTFF、块间输出延迟的 P50/P95、峰值显存、GPU-seconds/video，以及 drafter、target、cache 更新、VAE、reward、传输的耗时拆分。平均整段时间不足以说明流式体验。
- 同一 block 的路由结果会决定是否需要 target 以及后续历史；“CUDA streams 使 reward 不阻塞 denoising”需要时间线或 profiler 证据。若跨块提前执行，说明回滚成本。
- 记录 checkpoint、精度、scheduler、context noise、FPS、总帧数、软硬件版本与计时边界。当前协议是 9+8×12=105 像素帧，实际播放时长还取决于未明确的 FPS。

## 9. P2：最后修正写法和展示，避免可避免的审稿扣分

- “AR 结构消除了 exposure bias”（第 1 页）应改为具体训练策略如何减轻 train–inference mismatch；AR 本身并不消除该问题。
- “calibration-free”（第 3 页）与“calibrated offline”（第 4 页）统一。
- “partially denoised state is strictly more informative”（第 9 页）无证明不宜作普遍断言；更有信息不等于在大模型动力学下更有用。
- 不写 per-block quality guarantees 或 match target quality，除非补上相应验证或明确经验限定；本方法并未给出分布保真或逐块质量保证。
- 相同 seed 不等于相同生成结果。定性图中相似构图可能主要来自相同 target 首块，应加入首块 target 基线、块边界和路由决策标记。
- 图 1 caption 把左右两列 prompt 写成 top/bottom；表 3 第 k=3 行数值挤在一起；图 1、4、5 空白多且单帧太小。压缩空白、增加局部放大，并提供完整视频与失败案例。
- 主文应优先放强基线、独立质量验证与机制结果；普通定性样例和阈值全表可以放附录。

## 建议执行顺序

**第一阶段：确认方法是否站得住。** 审计 cache、随机数与 k=4 退化；统一 acceptance 口径；补首块保护下的 random/fixed/stitching 预算曲线；重画包含简单基线的主图。这一阶段决定是否保留“reward routing 是核心贡献”。

**第二阶段：确认质量与机制。** 对最有竞争力的少数配置做完整视频人评、配对统计、时序指标和反事实修复收益分析，不必对所有阈值同时扩展实验。

**第三阶段：确认范围并重写论文。** 补第二模型对、较长视频、冻结阈值迁移、性能拆分；按照实验真正支持的贡献重写摘要、引言与结论。

如果资源只够三项，优先：**强基线与贡献归因；完整视频质量验证；缓存和退化配置正确性检查。**

## 核对过的外部来源

- [RSD: Reward-Guided Speculative Decoding](https://arxiv.org/abs/2501.19324)：公开摘要明确使用 PRM、阈值与受控偏差。
- [HybridStitch](https://arxiv.org/abs/2603.07815)：公开摘要明确包含按难度划分空间区域并由大模型细化。
- [VisionReward](https://arxiv.org/html/2412.21059v3)：公开正文说明视觉判断与线性偏好建模；评分不自动等同于感知质量百分比。

以上建议旨在提高论证可信度与竞争力，不是接收概率估计；最终结论仍取决于新增实验结果和审稿判断。